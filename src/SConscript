# -*- python -*-

env = Environment()
env.ParseConfig("sdl-config --cflags --libs")
env.StaticLibrary('tinygettext', 
                  ['tinygettext.cpp',
                   'language_def.cpp',
                   'po_file_reader.cpp',
                   'dictionary.cpp',
                   'dictionary_manager.cpp',
                   'directory.cpp',
                   'findlocale.cpp'])


# EOF #
